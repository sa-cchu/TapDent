# TapDent プロジェクト開発規約

## 1. 全体方針
- 歯科予約システムを開発中。
- ユーザビリティ重視、診察券連携、不正予約制御という機能を持つ。
- 長文で回答せず、理解したことを完結に示すこと。情報が足りない場合は質問すること。
- Java21 / Spring Boot 4.0.6
- 実務的に正しい実装にすること
- DB処理で重たいものはクエリを使用すること

## 2. アーキテクチャ・フォルダ構成
- ディレクトリ構成は以下の通り:
  - config/ (初期化やセキュリティ設定)
  - .controller/ (リクエスト受付)
  - dto/ (データ転送用クラス)
  - entity/ (DBテーブル定義)
  - enums/ (ステータス・権限)
  - form/ (画面入力バリデーション)
  - repository/ (DB操作)
  - service/ (業務ロジック・チェック処理)

## 3. 実装ルール
- 不正予約制御など、DBが絡む整合性チェックはすべて Service 層に記述し、Controller から呼び出すこと。
- 論理削除があれば `is_deleted` フラグを使用し、Entity に `@SQLDelete` と `@Where` を付与すること。

## 4.フロントデザインルール
-Bootstrapをベースとする
-アクセシビリティ重視
-レスポンシブデザイン（スマホ、タブレットでの操作予定あり）
-テーマカラーはブルー
-UIはシンプルモダン